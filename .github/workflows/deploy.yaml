name: Deploy ON Push

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: action/checkout@v4
      - name: Health Check
        run: echo "Health Check Passed"